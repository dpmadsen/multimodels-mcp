# Registro de alterações de testes

## 2026-09-04T14:17:59-0700 — Escopo explícito de delegação

- Requisito/arquivos: `src/pasta-de-trabalho.test.ts` (novo grupo de testes), `src/tools/delegate.test.ts` (nova captura de registro), e adaptação do helper em `src/providers/claude-cli.test.ts` para fornecer o diretório explícito exigido pelo dispatcher. A produção correspondente está em `src/pasta-de-trabalho.ts`, `src/tools/delegate.ts`, `src/providers/codex.ts`, `src/providers/gemini.ts` e `src/providers/claude-cli.ts`.
- Testes: `resolve uma pasta existente para o caminho canônico`; `recusa workdir ausente ou inexistente`; `recusa um arquivo regular como workdir`; `registra delegate_task como não somente leitura e open-world`. O helper de `claude-cli.test.ts` passou `workdir: "."` às chamadas de handler que antes dependiam de cwd implícito.
- Motivo/comportamento protegido: o diretório delegado deve ser existente, canônico e diretório; modelos CLI com acesso a arquivos não podem iniciar sem `workdir`; `delegate_task` pode mutar arquivos e deve anunciar `readOnlyHint: false`, mantendo `openWorldHint: true`. A adaptação preserva as verificações existentes de effort/chave/modelo sem remover ou enfraquecer casos.
- Racional nos testes: comentário imediatamente acima do grupo lógico em `src/pasta-de-trabalho.test.ts:10-13`, em `src/tools/delegate.test.ts:7-10`, e helper explicitamente documentado em `src/providers/claude-cli.test.ts:91-96`. Cada comentário registra a exigência de consultar requisito, diff, histórico, memória, plano e este log antes de alterar a cobertura.
- Evidência de histórico: checkout `codex/security-hardening`, HEAD inicial `4fea6caa549e37e1fc8e7bbe0cd7236c3049d404`; `git log --oneline -- src/tools/delegate.ts src/providers/codex.ts src/providers/gemini.ts src/providers/claude-cli.ts` retornou `9f56f1b`, `ed3fc2e`, `a25de82`, `ffb84b0`, `858854a`, `0ac5fea`, `b08a3eb`, `a0164ca`; histórico dos testes retornou `9f56f1b`, `ed3fc2e`, `a25de82`, `ffb84b0`, `858854a`, `0ac5fea`, `a0164ca`. Busca `git log -S'workdir ?? process.cwd()' --oneline -- src/providers` retornou `ffb84b0`, `0ac5fea`, `a0164ca`; busca `git log -S'readOnlyHint' --oneline -- src/tools/delegate.ts` retornou `a0164ca`.
- Evidência de memória/plano: `rg` em `/Users/ppirooznia/.codex/memories/MEMORY.md` encontrou as regras de preservação de testes, log append-only e consulta de histórico (`MEMORY.md:12-39`). O requisito completo foi lido em `.superpowers/sdd/2026-09-04-multimodels-security-hardening/task-1-brief.md`; plano aprovado e arquivos não relacionados não foram alterados. Não havia histórico específico adicional apontado para este componente.
- TDD RED: `npm run build` falhou com `TS2307: Cannot find module './pasta-de-trabalho.js'`; em seguida `node --test dist/pasta-de-trabalho.test.js dist/tools/delegate.test.js dist/providers/codex.test.js dist/providers/gemini.test.js dist/providers/claude-cli.test.js` falhou pela ausência do resolver e pela anotação real `readOnlyHint: true` versus esperada `false` (42 passaram, 2 falharam). A falha foi esperada: comportamento novo ainda não existia.
- GREEN focado: `npm run build && node --test dist/pasta-de-trabalho.test.js dist/tools/delegate.test.js dist/providers/codex.test.js dist/providers/gemini.test.js dist/providers/claude-cli.test.js` — 46 testes passaram, 0 falharam.
- Validação completa: `npm test` — build TypeScript e suíte inteira concluídos com 277 testes passando e 0 falhando.
- Gate de provedores: `codex --version` = `codex-cli 0.153.0`; `codex exec --help` confirmou `--sandbox` (`read-only`, `workspace-write`, `danger-full-access`), `-C/--cd`, `--add-dir`, `--ephemeral`, `--ignore-user-config` e `shell_environment_policy` via configuração geral. `agy --help`/`agy --version` e `claude --help`/`claude --version` retornaram `command not found`; agy e claude permanecem `unverified`, sem modo gravável novo. Nenhum modelo externo foi executado e nenhum probe de permissão foi realizado.
- Limitações: MCP roots não foram tratados como sandbox; advisories transitivos ficaram fora do escopo. As funções de provider mantêm compatibilidade de tipo para chamadas diretas legadas com `undefined`, mas o dispatcher resolve e passa sempre a pasta canônica para as três raias CLI; não existe fallback `?? process.cwd()` na produção.

## 2026-09-04T14:30:00-0700 — Fix round 1: providers exigem workdir e capturam cwd

- Alterados `src/providers/codex.ts`, `src/providers/gemini.ts`, `src/providers/claude-cli.ts` para assinaturas `workdir: string`, guards defensivos antes do spawn e helper Claude com diretório obrigatório. `src/providers/claude-cli-processo.test.ts` agora usa diretório temporário explícito.
- Adicionados casos de entrada ausente e captura de `cwd` via executáveis falsos em `src/providers/codex.test.ts`, `src/providers/gemini.test.ts` e `src/providers/claude-cli-processo.test.ts`. O teste de Claude confirma caminho canônico via `realpath`.
- Racional: cobre diretamente a regressão apontada na revisão (spawn com cwd undefined usava o cwd do servidor) e prova que o processo recebe o diretório delegado. Nenhum teste existente foi removido ou enfraquecido.
- Evidência: focused `npm run build && node --test dist/pasta-de-trabalho.test.js dist/providers/codex.test.js dist/providers/gemini.test.js dist/providers/claude-cli.test.js dist/providers/claude-cli-processo.test.js` — 57/57; full `npm test` — 283/283.

## 2026-09-04T14:42:00-0700 — Fix round 2: isolamento do dublê agy e rationale

- Teste alterado: `passa o workdir explícito ao processo agy` em `src/providers/gemini.test.ts`. O teste agora usa `HOME` temporário, além de `PATH` temporário, e restaura ambos em `finally`; assim `resolveAgyBinary()` não pode preferir `~/.local/bin/agy` real. Se o isolamento fosse removido, a máquina com agy instalado poderia executar modelo externo.
- Rationale adicionado em `src/providers/codex.test.ts:76-80`, `src/providers/gemini.test.ts:79-84` e `src/providers/claude-cli-processo.test.ts:94-99`, cobrindo comportamento protegido, necessidade e revisão obrigatória de requisito/diff/histórico/memória/plano/changelog antes de modificar ou remover.
- Histórico/diff atual: revisão do commit anterior `a859a5e`; `git diff` desta rodada limitado aos três arquivos de teste e este log; histórico introdutório e posterior já registrado no primeiro round (`9f56f1b`, `ed3fc2e`, `a25de82`, `ffb84b0`, `858854a`, `0ac5fea`, `b08a3eb`, `a0164ca`). Memória consultada: `MEMORY.md:12-39`, com regras de rationale, histórico e log append-only. Plano/requisito: `task-1-brief.md` e report do Task 1.
- TDD seam correction: o risco pré-correção foi demonstrado por revisão do fluxo `resolveAgyBinary()` (prioriza `homedir()/.local/bin/agy` antes de PATH), sem executar CLI real; o caso atualizado falharia em ambiente com agy real ao retornar saída não correspondente do dublê. Nenhum modelo externo foi invocado.
- Validação: focused `npm run build && node --test dist/pasta-de-trabalho.test.js dist/providers/codex.test.js dist/providers/gemini.test.js dist/providers/claude-cli.test.js dist/providers/claude-cli-processo.test.js` — 57/57; full `npm test` — 283/283. Limitações: agy/claude reais indisponíveis nesta máquina e não há probe externo; isolamento do teste foi validado apenas pelo fake local e cwd capturado.

## 2026-09-04T14:48:00-0700 — Fix round 3: correção das referências de rationale

Esta entrada supersede as referências de linha imprecisas do fix round 2; o registro anterior permanece intacto por ser append-only.

- Testes novos exatos: `recusa workdir ausente antes de iniciar o Codex` e `passa o workdir explícito ao processo Codex` em `src/providers/codex.test.ts:92` e `src/providers/codex.test.ts:96`; `recusa workdir ausente antes de iniciar o agy` e `passa o workdir explícito ao processo agy` em `src/providers/gemini.test.ts:92` e `src/providers/gemini.test.ts:96`; `recusa workdir ausente antes de iniciar o Claude` e `passa o workdir explícito ao processo Claude` em `src/providers/claude-cli-processo.test.ts:107` e `src/providers/claude-cli-processo.test.ts:112`.
- Rationale atual: grupo Codex em `src/providers/codex.test.ts:88-90`; grupo Gemini em `src/providers/gemini.test.ts:88-91`; grupo Claude processo em `src/providers/claude-cli-processo.test.ts:95-98`. Esses comentários explicam comportamento protegido, necessidade e consulta obrigatória de requisito, diff, histórico, memória, plano e changelog.
- Verificação somente documental: `nl -ba` confirmou as linhas acima; `git diff --check` passou. Nenhum teste, produção ou comportamento foi alterado nesta rodada; nenhuma nova execução externa ocorreu.

## 2026-09-04T14:41:44-0700 — Isolamento de credenciais nos filhos CLI

- Requisito/arquivos: `src/providers/ambiente-filho.ts` e `src/providers/ambiente-filho.test.ts` novos; `src/providers/codex.ts`, `src/providers/gemini.ts`, `src/providers/claude-cli.ts`, `src/providers/claude-cli.test.ts` e `src/providers/claude-cli-processo.test.ts` alterados. Todos os três spawns agora recebem ambiente explícito. A lista base é estrita; somente Codex adiciona `CODEX_HOME`; Claude por assinatura adiciona `API_TIMEOUT_MS`; Claude com chave adiciona somente diretório, rota e credencial selecionados.
- Testes/motivo protegido: o grupo novo em `ambiente-filho.test.ts` usa valores sentinela para provar que segredos de AWS, OpenRouter, z.ai, DeepSeek e valor não relacionado não atravessam para base, Codex, Gemini ou Claude, e que ausência nunca vira o texto `undefined`. O smoke do processo em `claude-cli-processo.test.ts` prova o ambiente efetivamente recebido pelo filho Claude com chave. `claude-cli.test.ts` prova a divisão de ferramentas: assinatura retém `Read`/`Glob`/`Grep` e os dois Bash npm; chave API recebe somente leitura. Isso evita gasto/API cruzada e execução npm com credencial de terceiro.
- Racional nos testes: comentário de isolamento em `src/providers/ambiente-filho.test.ts:1-3`; comentário de ferramentas por raia em `src/providers/claude-cli.test.ts:29-30`; comentário de smoke do spawn em `src/providers/claude-cli-processo.test.ts:126-127`. Todos exigem a revisão de requisito, diff, histórico, memória e plano antes de enfraquecer a cobertura, conforme AGENTS.md. Os antigos testes de ambiente Claude foram substituídos pelo grupo completo; o registro de remoção, razão e cobertura substituta permanece em `src/providers/claude-cli.test.ts:305-308`.
- Histórico/memória/plano: estado inicial `codex/security-hardening` com documentação não rastreada somente fora do escopo. `git log --oneline -- src/providers/claude-cli.ts src/providers/claude-cli.test.ts src/providers/claude-cli-processo.test.ts` retornou `ad6e13b`, `a859a5e`, `6f4c3fe`, `9f56f1b`, `ed3fc2e`, `ffb84b0`. `git show --find-renames ed3fc2e` registrou que a assinatura depende de `~/.claude`, que config descartável produz `Not logged in`, e que as variáveis Anthropic herdadas antes precisavam ser removidas para evitar cobrança API. A busca em `MEMORY.md` por ambiente/credencial/Claude não trouxe registro específico deste repositório; essa ausência é a limitação de memória. Foi lido `task-2-brief.md`; nenhum modelo externo ou chave real foi usado.
- TDD RED: `npm run build && node --test dist/providers/ambiente-filho.test.js dist/providers/claude-cli.test.js dist/providers/claude-cli-processo.test.js` compilou e falhou como esperado: 26 passaram e 7 falharam. As falhas confirmaram `UNRELATED_SECRET`/segredos ainda herdados e `Bash(npm test:*)`/`Bash(npm run build:*)` ainda liberados na raia com chave. O smoke separado do processo, após correção de sintaxe do dublê, teve 8 passantes e 1 falha esperada porque `UNRELATED_SECRET` alcançava o filho.
- GREEN focado: `npm run build && node --test dist/providers/ambiente-filho.test.js dist/providers/codex.test.js dist/providers/gemini.test.js dist/providers/claude-cli.test.js dist/providers/claude-cli-processo.test.js` — 54 testes passaram, 0 falharam. `npm test` — 283 testes passaram, 0 falharam. `git diff --check` passou. O runner não imprimiu valores sentinela nem segredos.
- Limitações: testes usam apenas dublês locais e valores sentinela; CLIs/modelos reais não foram executados. A decisão de não conceder Bash/npm à raia com chave permanece até desenho separado e aprovado de proxy de credenciais; nenhum proxy ou serviço de credenciais foi criado.

## 2026-09-04T14:47:52-0700 — Correção do registro de remoção dos testes Claude

- Alteração somente documental: o comentário de registro em `src/providers/claude-cli.test.ts` agora enumera individualmente os sete nomes removidos e mapeia cada um para os testes substitutos em `src/providers/ambiente-filho.test.ts`. Nenhuma produção, teste executável ou comportamento foi alterado.
- Nomes antigos e substituições: `assinatura: as variáveis ANTHROPIC herdadas são removidas do ambiente do filho` -> `Claude por assinatura preserva o login em HOME e somente seu prazo`; `assinatura: nenhuma variável do filho fica com o texto 'undefined'` -> `ambientes filhos nunca serializam valor ausente como texto undefined`; `assinatura: não define CLAUDE_CONFIG_DIR (pasta descartável dá 'Not logged in')` -> `Claude por assinatura preserva o login em HOME e somente seu prazo`; `assinatura: o filho continua herdando o básico do sistema (PATH)` -> `ambiente base mantém só o sistema permitido e exclui segredos de outros provedores`; `raia de fabricante: define endereço, chave e pasta descartável` -> `Claude com chave adiciona somente a rota e credencial selecionadas`; `raia de fabricante: o endereço herdado do ambiente é sobrescrito pelo do cardápio` -> `Claude com chave adiciona somente a rota e credencial selecionadas`; `as duas raias dão ao Claude Code 1 minuto a mais que o nosso prazo` -> os dois testes Claude por assinatura/com chave citados acima.
- Rationale atual: o grupo de isolamento continua em `src/providers/ambiente-filho.test.ts:1-3`; o registro individual de remoção fica imediatamente antes do teste de cardápio em `src/providers/claude-cli.test.ts`. Ambos preservam a exigência de AGENTS.md de revisar requisito, diff, histórico, memória e plano antes de reduzir cobertura.
- Base de histórico/memória/plano: conferidos `git show 50c6737 -- src/providers/claude-cli.test.ts`, o registro Task 2 anterior e `task-2-brief.md`; a busca atual em `MEMORY.md` por environment/credential/claude-cli não trouxe memória específica deste repositório. O finding fornecido pelo revisor é a autoridade desta correção mínima.
- Verificação: `git diff --check` e inspeção de linha-fonte com `nl -ba src/providers/claude-cli.test.ts` após a alteração. Sem execução de testes, pois o JavaScript/TypeScript executável não mudou.

## 2026-09-04T14:50:43-0700 — Restauração das três travas Anthropic da assinatura

- Asserções restauradas: o teste substituto `Claude por assinatura preserva o login em HOME e somente seu prazo` em `src/providers/ambiente-filho.test.ts` agora exige a ausência explícita de `ANTHROPIC_API_KEY`, `ANTHROPIC_AUTH_TOKEN` e `ANTHROPIC_BASE_URL`. As duas últimas foram restauradas nesta rodada; a primeira já existia.
- Contrato antigo preservado: a origem é o teste removido `assinatura: as variáveis ANTHROPIC herdadas são removidas do ambiente do filho`, que verificava os mesmos três nomes. A rationale continua no grupo de isolamento em `src/providers/ambiente-filho.test.ts:1-3`; o registro individual de remoção em `src/providers/claude-cli.test.ts:304-321` aponta esse teste como sua substituição.
- Base de histórico/memória/plano: conferidos o diff histórico de `50c6737` que removeu o teste, o registro de remoção de `20068e1`, `task-2-brief.md` e a busca atual em `MEMORY.md` por environment/credential/claude-cli, sem memória específica deste repositório. O finding do revisor autoriza esta restauração mínima; nenhuma produção foi alterada.
- Validação: focused `npm run build && node --test dist/providers/ambiente-filho.test.js` e full `npm test`, além de `git diff --check`. Não há RED de produção: a implementação já era fail-closed; a mudança restaura cobertura exigida.

## 2026-09-04T15:04:55-0700 — Limites de resposta, redirects e logs seguros

- Resolução de limites: `src/config.test.ts` cobre `resolveMaxOutputTokens` e `resolveMaxResponseBytes` (modelo -> provedor -> defaults -> padrão), `contextTokens` somente como metadado, modelo Codex ausente e valores zero/negativo/fracionário/não seguro. O comentário imediatamente acima do grupo exige requisito/diff/histórico/MEMORY/plano/log antes de redução. Necessário para não confundir contexto com limite aplicado nem aceitar config insegura.
- Corpo HTTP limitado: `src/providers/openai-compat.test.ts` cobre resposta exatamente no teto, chunks que ultrapassam e são cancelados, e UTF-8 multibyte partido. A mesma rationale em `src/providers/openai-compat.test.ts` protege a contagem de bytes, não caracteres; `src/providers/limite-saida.test.ts` cobre o helper puro equivalente dos streams CLI.
- Redirect: `src/providers/openai-compat.test.ts` exige `redirect: "manual"`, recusa 302/307 sem retry nem `Location`, e preserva uma única retry para rede, 429 e 5xx; `src/panel/catalog.test.ts` exige o redirect manual no GET do LM Studio. Nenhuma allowlist foi introduzida e os endpoints iniciais continuam configuráveis.
- Limites CLI: `src/providers/codex.test.ts` prova `stat()` antes de ler arquivo maior; `src/providers/claude-cli-processo.test.ts` prova que o menor limite encerra o stream local falso; `src/providers/limite-saida.test.ts` protege bytes UTF-8. Os dublês locais evitam CLI/modelo/chave reais. Comentários nos grupos registram a revisão obrigatória de requisito, diff, histórico, memória, plano e este log.
- Logger: `src/observabilidade.test.ts` captura `console.error`, exige JSONL e rejeita `Error` com prompt/resposta/Bearer/URL; `src/tools/delegate.test.ts` prova providerId de produção e Codex sem modelo; `src/tarefas/execucao.test.ts` prova criação/desfecho sem texto sensível. Os comentários desses grupos registram o motivo e a política de histórico. As mensagens fixas preexistentes de startup/falha de storage não foram alteradas.
- Evidência de histórico/memória: estado inicial `codex/security-hardening` tinha somente `docs/security-hardening/`, `docs/security-report.md` e `docs/superpowers/` não rastreados; `git log` nos arquivos de produção/teste relevantes retornou `9f56f1b`, `ed3fc2e`, `ffb84b0`, `0ac5fea`, `a0164ca` (e `fba96ac` nos testes). `git log -S'DEFAULT_MAX_TOKENS'` e `-S'ESPERA_REPESCAGEM_MS'` retornaram `a0164ca`. A busca em `MEMORY.md` confirmou em `101` e `108` que redirects devem ser desativados antes de leitura; não havia memória específica deste repo para estes componentes. O brief completo Task 3 foi a autoridade; não houve remoção/enfraquecimento de teste.
- TDD: RED com `npm run build` falhou como esperado por exports/módulos/campos ainda ausentes (`resolveMax*`, `lerCorpoLimitado`, `observabilidade`, `limite-saida`); depois da implementação, o focused `npm run build && node --test ...` passou 63/63 na última repetição e o slice Gemini final passou 12/12 (o conjunto completo focado anterior passou 134/134 antes dos quatro casos finais, que também passaram nos slices repetidos). Sem chamadas a provedores reais ou chaves.

## 2026-09-04T15:14:58-0700 — Correção 1/5: resolução de limites

- Testes exatos: `limite de tokens usa modelo, depois provedor e por fim o padrao`, `limite de bytes usa modelo, provedor, defaults e padrao`, `limites invalidos falham fechados` e `codex sem modelo usa o limite declarado pelo provedor` em `src/config.test.ts`.
- Rationale: comentário imediatamente acima do grupo de limites em `src/config.test.ts`; protege cascata fail-closed e deixa `contextTokens` somente como metadado. Antes de mudar/remover exige requisito, diff, histórico, MEMORY.md, plano e este log.
- Histórico/memória: histórico atual `36f7704`, `50c6737`, `a859a5e`; `MEMORY.md:101,108` confirma a postura fail-closed na fronteira remota. O brief Task 3 e o finding desta correção são a autoridade; não há memória específica adicional para esta config.
- Comandos/resultados: RED focado compilou e falhou por comportamentos Task 3 ainda ausentes; GREEN `npm run build && node --test dist/providers/gemini.test.js dist/providers/claude-cli-processo.test.js dist/providers/openai-compat.test.js dist/observabilidade.test.js` passou 54/54. Limitação: não houve catálogo/configuração de limites pela UI, por estar fora de escopo.

## 2026-09-04T15:14:58-0700 — Correção 1/5: corpo HTTP limitado

- Testes exatos: `le corpo exatamente no limite e preserva UTF-8 partido entre chunks`, `cancela stream quando o corpo passa um byte do limite`, `corpos grandes de sucesso ou erro falham sem repescagem` e `retry seguida de corpo grande registra reject de bytes sem terceira tentativa` em `src/providers/openai-compat.test.ts`.
- Rationale: comentário de fronteira HTTP em `src/providers/openai-compat.test.ts`; protege byte accounting, cancelamento e ausência de buffer além do limite. A revisão obrigatória de requisito/diff/histórico/memória/plano/log está no comentário.
- Histórico/memória: `git log --oneline -- src/providers/openai-compat.ts` inclui `36f7704` e `a0164ca`; `MEMORY.md:101,108` confirma rejeição antes de aceitar corpo remoto. Comando GREEN acima passou 54/54; RED tinha os casos retry-segundo-corpo e Claude falhando pelo comportamento ausente. Limitação: usa `fetch` falso/local, sem chamada a provedor real.

## 2026-09-04T15:14:58-0700 — Correção 1/5: redirects

- Testes exatos: `completion usa redirect manual`, `redirects 302 e 307 falham sem repescagem nem Location` e `retry seguida de redirect registra reject antes do finish, sem terceira tentativa` em `src/providers/openai-compat.test.ts`.
- Rationale: mesmo comentário de fronteira HTTP em `src/providers/openai-compat.test.ts`; protege ordem `start,retry,reject,finish`, status sem `Location` e duas tentativas no máximo.
- Histórico/memória: `36f7704` é a implementação introduzida; `MEMORY.md:101,108` exige no-redirect antes de leitura. RED focado registrou a ausência do `provider.reject` na segunda tentativa; GREEN 54/54. Limitação: inicial endpoint configurado continua permitido e não foi introduzida allowlist.

## 2026-09-04T15:14:58-0700 — Correção 1/5: limites CLI

- Testes exatos: `Gemini encerra stdout que ultrapassa o limite resolvido` e `Gemini rejeita um unico chunk acima do limite sem expor seu texto` em `src/providers/gemini.test.ts`; `Claude encerra o stream ao ultrapassar o menor limite resolvido` e `Claude descarta texto do chunk que ultrapassa o limite antes de parsear` em `src/providers/claude-cli-processo.test.ts`.
- Rationale: comentários imediatamente acima dos novos grupos Gemini/Claude; protegem que o chunk excedente não entra em `stdout`, cauda diagnóstica, parser ou parcial persistível e que eventos posteriores retornam cedo após kill.
- Histórico/memória: `git log` nos dois providers inclui `36f7704`, `50c6737`, `a859a5e`; o finding Critical desta rodada é a autoridade específica. RED falhou no Claude porque `DADO_EXCEDENTE_NAO_RETER` já entrava no parcial; GREEN 54/54. Limitação: dublês locais apenas; agy/claude/modelos reais não foram executados.

## 2026-09-04T15:14:58-0700 — Correção 1/5: logger seguro

- Testes exatos: `cada evento sai como uma unica linha JSON sem dados sensiveis arbitrarios` em `src/observabilidade.test.ts`, mais as duas regressões de segunda tentativa em `src/providers/openai-compat.test.ts`.
- Rationale: comentário no início de `src/observabilidade.test.ts` fecha o vocabulário e impede prompt, resposta, Bearer e URL; comentário de segunda tentativa protege a chamada única de rejeição tipada antes de embrulhar o erro.
- Histórico/memória: `36f7704` introduziu o logger; `MEMORY.md:101,108` reforça que logs não devem depender do destino final após redirect. GREEN 54/54 confirmou JSONL e ordem segura; RED mostrou `start,retry,finish` sem reject no segundo redirect. Limitação: não foram alteradas mensagens stderr legadas de startup/storage.

## 2026-09-04T15:14:58-0700 — Correção 1/5: redirect de catálogo

- Teste exato: `consulta ao catalogo LM Studio usa redirect manual` em `src/panel/catalog.test.ts`.
- Rationale: comentário imediatamente acima do teste exige requisito/diff/histórico/MEMORY/plano/log e protege o GET de catálogo sem alterar endpoint inicial.
- Histórico/memória: `git log --oneline -- src/panel/catalog.ts` inclui `36f7704`, `9f56f1b`, `ed3fc2e`; `MEMORY.md:101,108` é a evidência de no-redirect. O GREEN final completo abaixo cobre este caso. Limitação: catalog body continua sem byte cap por decisão explícita do brief.

## 2026-09-04T15:14:58-0700 — Correção 1/5: ciclo de vida de tarefas

- Teste exato: `tarefa em segundo plano registra criacao e desfecho sem texto sensivel` em `src/tarefas/execucao.test.ts`.
- Rationale: comentário imediatamente acima protege os dois eventos de ciclo de vida sem texto de pedido/resposta e obriga revisão de requisito/diff/histórico/MEMORY/plano/log antes de alteração.
- Histórico/memória: `git log --oneline -- src/tarefas/execucao.ts` inclui `36f7704`, `9f56f1b`, `ed3fc2e`; não há memória específica adicional, e `MEMORY.md:101,108` foi a evidência geral de boundary logging. GREEN focado 54/54; `npm test` final será registrado no report desta rodada. Limitação: esta correção não altera persistência, fila ou prazo de tarefas.

## 2026-09-04T15:19:28-0700 — Correção 2/5: telemetria Claude usa total pós-chunk

- Teste exato: `Claude descarta texto do chunk que ultrapassa o limite antes de parsear` em `src/providers/claude-cli-processo.test.ts` agora exige `observedBytes > 4000` e `limitBytes === 4000`; o comentário imediatamente acima explica que protege parser/parcial e a telemetria do total pós-chunk, e exige revisão de requisito, diff, histórico, MEMORY.md, plano e este log antes de alteração.
- Histórico/memória: `git log --oneline -- src/providers/claude-cli.ts src/providers/claude-cli-processo.test.ts` retornou `e85c7dd`, `36f7704`, `50c6737`; `MEMORY.md:101,108` confirma a postura fail-closed de fronteira. O finding Important desta rodada é a autoridade específica; não há memória Claude adicional.
- TDD: RED `npm run build && node --test dist/providers/claude-cli-processo.test.js` compilou e falhou 10/11 com `precisa incluir o chunk que cruzou o teto`, provando que `observedBytes` ainda era o total anterior. GREEN `npm run build && node --test dist/providers/claude-cli-processo.test.js dist/observabilidade.test.js` passou 12/12. Limitação: usa somente dublê local; nenhum CLI/modelo/credencial real foi executado.

## 2026-09-04T15:23:35-0700 — Task 4: bloqueio de mutações cross-site do painel

- Testes adicionados em `src/panel/mutation-guard.test.ts`: aceitação de `Origin` exato (`http://127.0.0.1:4747`), `Host` exato e `application/json` com charset; rejeição de Origin ausente/incorreto, Host ausente/incorreto e Content-Type ausente, `text/plain` ou formulários. A rationale fica no comentário antes do grupo de testes: protege a autoridade de mutação do painel do usuário e bloqueia pedidos cross-site antes da persistência; exige revisão completa antes de enfraquecer ou remover a cobertura.
- Histórico/requisito consultados: `task-4-brief.md`; `git log --oneline -- src/panel/server.ts src/panel/config-write.ts ui/src/lib/api.ts` e `git log --oneline -- src/panel/config-write.test.ts src/panel/provider-view.test.ts`; `git log -S'127.0.0.1' --oneline -- src/panel README.md`; diff atual dos arquivos de painel e busca em `MEMORY.md` por painel local/CSRF/Origin/test history. O Task 3 e o logger fechado `panel.reject` foram preservados como autoridade para rejeições sem dados sensíveis.
- TDD: RED `npm run build && node --test dist/panel/mutation-guard.test.js` falhou na compilação porque `mutation-guard.js` ainda não existia; após a implementação, GREEN `npm run build && node --test dist/panel/mutation-guard.test.js` passou 4/4. Validação focada `npm run build && node --test dist/panel/mutation-guard.test.js dist/panel/config-write.test.js dist/panel/env-file.test.js dist/panel/provider-view.test.js` passou 61/61.
- Limitação: os testes são puros e não fazem mutação no config real nem chamadas a provedores/chaves reais; processos locais do mesmo usuário ainda podem forjar headers de navegador. A validação é aplicada apenas às rotas POST, sem alterar GET, estáticos, destinos HTTP/HTTPS elegíveis, CORS, token ou autenticação.

## 2026-09-04T15:56:41-0700 — Onda final: restrição real do Claude e governança de contratos

- Registro superseding desta onda: o diff corrente partiu de `01555c0` limpo em `codex/security-hardening` e alterou `src/providers/claude-cli.ts`, `src/providers/claude-cli.test.ts`, `src/providers/claude-cli-processo.test.ts`, `src/tools/delegate.ts`, `src/tools/delegate.test.ts` e `src/providers/openai-compat.test.ts`. O finding final mostrou que `--allowedTools` apenas pré-aprova chamadas; por isso, a raia Claude com chave agora também exige `--tools Read,Glob,Grep` e `--permission-mode dontAsk`. A assinatura conserva a receita anterior, inclusive os dois Bash npm.
- Testes exatos da restrição: `a raia com chave restringe as ferramentas disponíveis a Read, Glob e Grep` e `a assinatura conserva as ferramentas de verificação, enquanto a chave fica só na leitura`, em `src/providers/claude-cli.test.ts`. A rationale imediatamente acima do primeiro explica a diferença entre disponibilidade e pré-aprovação e exige revisão de requisito, diff, histórico, memória, plano e deste log; o segundo prova que os argumentos de assinatura não receberam `--tools`/`dontAsk` e que continuam com `Bash(npm test:*)` e `Bash(npm run build:*)`.
- Contrato de registro exato: `descreve workdir como obrigatório em toda raia CLI e ignorado nas APIs diretas`, em `src/tools/delegate.test.ts`, prova que o schema continua aceitando ausência estruturalmente, mas orienta que Codex, Gemini e toda raia CLI/com mãos exigem `workdir`, enquanto APIs diretas o ignoram. A rationale fica imediatamente acima do teste e registra o prejuízo de voltar à descrição parcial `glm-maos`.
- Racionais restaurados para mudanças lógicas anteriores: `a requisição envia max_tokens (padrão e configurado)` em `src/providers/openai-compat.test.ts` documenta que `maxOutputTokens` é a dica wire `max_tokens`, separada de `contextTokens` e `maxResponseBytes`; `erro 429 preserva uma unica repescagem` documenta a única tentativa extra preservada. Em `src/providers/claude-cli.test.ts`, o comentário antes de `sem a chave no .env dá erro amigável apontando a variável, antes de qualquer spawn` cobre também `chave presente porém vazia conta como chave faltando`, `sem modelo explícito dá erro amigável (a receita exige --model)` e `a raia de assinatura continua exigindo modelo explícito`: o `workdir` existente só permite alcançar a validação sob teste. Em `src/providers/claude-cli-processo.test.ts`, o comentário imediatamente antes do helper `WORKDIR` explica o cwd explícito compartilhado; o grupo `execução que dá certo devolve o texto do evento result`/`recusa workdir ausente antes de iniciar o Claude`/`passa o workdir explícito ao processo Claude` mantém a prova específica da fronteira.
- Evidência consultada: requisitos finais e `task-6-brief.md`; plano `docs/superpowers/plans/2026-09-04-multimodels-security-hardening.md`; relatório/spec corrigidos; diff completo; commits de introdução e posteriores `6f4c3fe`, `a859a5e`, `ad6e13b`, `50c6737`, `36f7704`, `e85c7dd`, `2c9486b`, `0133f9a`, `ab5b486`, `6720a9d`, `01555c0`. `MEMORY.md:28-35` exige rationale específica, histórico completo e registro append-only; não foi encontrado registro específico de multimodels no índice de memória. A referência oficial da Anthropic `https://code.claude.com/docs/en/cli-reference` define `--allowedTools` como pré-aprovação e `--tools` como restrição de disponibilidade; `https://code.claude.com/docs/en/permission-modes` define `dontAsk` como negação automática fora do pré-aprovado em execução não interativa.
- TDD e validação: RED `npm run build && node --test dist/providers/claude-cli.test.js dist/tools/delegate.test.js` compilou e falhou 3/24 exatamente pela ausência de `--tools`/`dontAsk` e pela descrição antiga de `workdir`. GREEN do mesmo comando passou 24/24. GREEN focado com `dist/providers/claude-cli.test.js`, `dist/providers/claude-cli-processo.test.js`, `dist/tools/delegate.test.js` e `dist/providers/openai-compat.test.js` passou 64/64. Regressão `npm test` passou 313/313. Nenhum teste foi removido, pulado ou enfraquecido.
- Limitações: somente argumentos puros e processos/fetches locais falsos foram usados; nenhum CLI/modelo/provedor/chave real foi chamado. A verificação nativa de confinamento gravável continua aberta. A restrição de ferramentas cobre a raia com chave; a assinatura mantém deliberadamente sua capacidade de `npm test`/`npm run build`.

## 2026-09-04T17:12:09-0700 — Major: bloquear hooks de projeto no Claude com chave

- Autoridade e estado inicial: correção Major+ autorizada pelo usuário na revisão de segurança; checkout `/private/tmp/multimodels-security-hardening`, branch `codex/security-hardening`, HEAD `c4d2dd3e8c563152b17b73eeda5fd1dee658d9d0`, sem alterações iniciais. A opção `--tools` não bloqueava comandos de hooks de inicialização com a credencial selecionada. O requisito preserva leitura, endpoint aprovado e receita da assinatura.
- Mudança lógica 1: `a raia com chave restringe as ferramentas disponíveis a Read, Glob e Grep`, em `src/providers/claude-cli.test.ts`, agora exige `--settings` com `disableAllHooks:true`. Rationale imediatamente acima do teste: impede comandos automáticos fora de `--tools` e exige revisar requisito, diff, histórico, memória, plano e log antes de modificar/remover.
- Mudança lógica 2: `a assinatura conserva as ferramentas de verificação, enquanto a chave fica só na leitura`, no mesmo arquivo, também exige ausência de `--settings` na assinatura. Rationale imediatamente acima: protege as automações existentes da assinatura e exige a mesma revisão completa antes de alteração/remoção.
- Mudança lógica 3: `a raia com chave entrega ao processo só sua credencial e rota selecionadas`, em `src/providers/claude-cli-processo.test.ts`, e seu modo de dublê `ambiente` agora incluem argv na resposta. O teste confere que o processo que recebe a chave selecionada também recebe o JSON que desliga hooks; todas as asserções existentes de credenciais continuam. Rationale acima do teste e referência no dublê: prova a fronteira real do spawn, não a implementação interna do Claude, e obriga revisar também o log antes de modificar/remover.
- Histórico consultado: diff atual e diff `4fea6ca..c4d2dd3` dos testes; `git log --follow` identifica a introdução dos argumentos em `ffb84b0`, assinatura em `ed3fc2e`, processo em `9f56f1b`, além do squash atual. Inspecionados commits relevantes `ed3fc2e`, `9f56f1b`, `50c6737` (ambiente/sentinelas) e `bbffffa` (restrição de ferramentas e rationale), ainda disponíveis como objetos locais. Plano `docs/superpowers/plans/2026-09-04-multimodels-security-hardening.md`, seção de credenciais, requisitos de assinatura e histórico append-only foram consultados. Busca em `MEMORY.md` por multimodels/Claude hooks não retornou memória específica; nenhuma razão histórica foi inventada. Nenhum teste foi removido, renomeado, pulado ou enfraquecido; o diff completo de testes foi revisado.
- Evidência oficial: `https://code.claude.com/docs/en/hooks#disable-or-remove-hooks` e `https://code.claude.com/docs/en/settings#settings-precedence` confirmam que `--settings` prevalece sobre os arquivos de projeto/local, inclusive `disableAllHooks:false`. Políticas administradas têm precedência superior e continuam fora desta proteção.
- TDD: `timeout 60 npm run build && timeout 60 node --test dist/providers/claude-cli.test.js dist/providers/claude-cli-processo.test.js` compilou e falhou 2/31 antes da implementação, exatamente por faltar a opção de hooks nos argumentos e no processo. O mesmo comando passou 31/31 após a correção. Regressão `timeout 120 npm test` passou 313/313, sem skips, em 16,27 segundos de testes; saída em `/private/tmp/multimodels-hooks-npm-test.log`. `git diff --check` passou.
- Limitações: `command -v claude` não encontrou o binário. Não houve instalação, chamada paga nem uso de credencial real. A supressão interna de hooks não foi demonstrada em Claude nativo; a evidência executável cobre o contrato de argumentos no processo filho e a compatibilidade da assinatura. A verificação nativa de confinamento gravável continua aberta.
