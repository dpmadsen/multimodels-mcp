# Current handoff

Follow-up to the Major finding on squashed revision `c4d2dd3`: keyed Claude now sends `--settings '{"disableAllHooks":true}'` through the shared launch path, closing project/local hook execution outside the tool allowlist. Subscription settings remain unchanged. Administrator-managed hooks/policy retain Claude's documented precedence. Focused argv and spawned-child checks passed 31/31 and full `npm test` passed 313/313; native suppression is unverified because `claude` is unavailable. The independent review also noted a Minor response-body cancellation gap on early size/redirect rejection; it remains outside the authorized Major+ fix scope.

The security hardening work is on `codex/security-hardening`. Read [AGENTS.md](AGENTS.md), [README.md](README.md), the [corrected security report](docs/security-report.md), the [implementation plan](docs/superpowers/plans/2026-09-04-multimodels-security-hardening.md), and the append-only [test change log](docs/test-change-log.md).

Verified milestone commits: `6f4c3fe`, `a859a5e`, `ad6e13b`, `f921c54`, `50c6737`, `20068e1`, `3c6c4dc`, `36f7704`, `e85c7dd`, `2c9486b`, `0133f9a`. Final-review corrections are in `bbffffa`: focused tests passed 64/64 and full `npm test` passed 313/313. The exact RED/GREEN evidence and test rationales are recorded in the report and test log.

The current source keeps `workdir` optional in the shared input, requires and canonicalizes it for every CLI lane, and ignores it for direct APIs. It verifies minimal child environments, keyed-Claude availability restricted to `Read`/`Glob`/`Grep` with `dontAsk`, subscription Claude retaining `npm test`/`npm run build`, manual redirects, byte limits with a 10 MiB fallback, exact-origin panel mutations, and safe JSONL stderr events. Approved code transfer, arbitrary approved endpoints, and shared all-project task history remain intentional.

The scoped re-review of `bbffffa` passed: all six final findings are addressed, with no new Critical/Important breakage. The fix-wave evidence remains focused tests 64/64 and full `npm test` 313/313; the controller also freshly verified 313/313 at `14dd609`. Native writable CLI confinement is still unverified and open. Deferred items remain the Codex/Gemini child-observed sentinel tests, panel HTTP integration test, and transitive SDK advisories. The final memory note is recorded at `/Users/ppirooznia/.codex/memories/extensions/ad_hoc/notes/20260904T160959-0700-multimodels-security-final.md`. Next safe action: branch integration decision. Do not claim main integration, writable agents, deploy, delete, or publish.
